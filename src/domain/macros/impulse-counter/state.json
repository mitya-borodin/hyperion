{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/CounterMacrosPublicState",
  "definitions": {
    "CounterMacrosPublicState": {
      "type": "object",
      "properties": {
        "amount": {
          "type": "number",
          "description": "Значение в единицах измерения, которое пользователь может задать, чтобы синхронизовать значение физического счетчика и счетчика импульсов."
        }
      },
      "required": ["amount"],
      "additionalProperties": false,
      "description": "! STATE"
    }
  }
}
