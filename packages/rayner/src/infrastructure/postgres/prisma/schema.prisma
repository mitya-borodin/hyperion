generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./hyperion.db"
}

model Settings {
  name  String @unique
  value String

  @@map("_settings")
}

model HyperionDevice {
  id String @id @default(uuid())
}
